[build]
  publish = "public"
  command = "npm install --legacy-peer-deps && npm run build"
[build.environment]
  NODE_VERSION = "20"

# Redirect Netlify Identity setelah login
[[redirects]]
  from = "/"
  to = "/admin/"
  status = 200
  conditions = {Role = ["admin"]}
